Superseded 2026-08-18. These 6 SVGs were rendered from Mermaid source found in
a Notion "Interview Diagram Prep" page — the best source available at the time,
but a slightly-stale draft (missing the Auto-Touch feature entirely).

Replaced same-day by 13 SVGs exported directly from the real, code-verified
`docs/diagrams/lumine_diagrams.drawio` in the `project-lumine` repo
(`develop` branch), which Lumine's own Oracle built and verified against the
live codebase across several sessions. See `src/content/site.ts` for the
current set.

Kept per this project's diagram convention (CLAUDE.md, "Nothing is Deleted"):
prior exports are archived, not deleted, when a diagram is re-exported.
